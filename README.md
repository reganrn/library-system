# library-system
Console library management system in Java (JDBC) with core business logic implemented in Oracle PL/SQL - atomic borrowing, loan limits, and fine calculation enforced at the database level.
